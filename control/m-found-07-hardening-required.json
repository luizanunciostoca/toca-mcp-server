{
  "milestone": "M-FOUND-07",
  "status": "HARDENING_REQUIRED_BEFORE_MERGE",
  "required_fixes": [
    "workflow domain events use a durable eventKey to prevent same-type/version collisions",
    "stale delivery recovery verifies the claimed attempt row was updated",
    "in-memory PoolClient placeholders satisfy strict lint without weakening interfaces"
  ],
  "merge_rule": "Any controller frozen before this commit must abort and be rerun after a new exact-head green Quality Gate."
}
