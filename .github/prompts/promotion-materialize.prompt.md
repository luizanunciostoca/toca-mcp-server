# TOCA OS — PRO+ v2 Promotion Materialization

Prefer promotion candidates created on demand after prerequisites pass.

Revalidate current main and prerequisite evidence, create a fresh branch from current main, apply only the exclusive promotion diff, run fresh CI, and require the separate promotion authorization before any promotion. A long-lived draft is an exception and must never be treated as current merely because it remains open.
