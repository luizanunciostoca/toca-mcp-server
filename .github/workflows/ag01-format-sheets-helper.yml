name: AG-01 Format Sheets Helper

on:
  push:
    branches: [ops/ag01-vertex-permanent-binding-20260828]
    paths: ['.github/workflows/ag01-format-sheets-helper.yml']

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: true
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1
        with:
          version: 10.15.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 24
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write src/ag01-sheets-readiness-smoke.ts
      - name: Commit canonical formatting
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo 'No formatting changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/ag01-sheets-readiness-smoke.ts
          git commit -m 'style(ag01): apply canonical Prettier formatting'
          git push origin HEAD:ops/ag01-vertex-permanent-binding-20260828
