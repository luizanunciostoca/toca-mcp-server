name: Meta Ads The Party Failure Diagnostic

on:
  push:
    branches:
      - main
    paths:
      - control/meta-ads-the-party-2026-08-15.diagnose

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: toca-mcp-production
  GCP_WORKLOAD_IDENTITY_PROVIDER: projects/990081828836/locations/global/workloadIdentityPools/github/providers/github-toca-mcp
  GCP_DEPLOY_SERVICE_ACCOUNT: toca-mcp-deployer@toca-mcp-production.iam.gserviceaccount.com
  EXPECTED_MARKER: INSPECT_META_ADS_RECONCILIATION_31778980116
  RECON_JOB_NAME: toca-meta-ads-reconcile-31778980116

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Validate read-only diagnostic trigger
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF" = "refs/heads/main"
          test -f control/meta-ads-the-party-2026-08-15.diagnose
          test "$(tr -d '\r\n' < control/meta-ads-the-party-2026-08-15.diagnose)" = "$EXPECTED_MARKER"
          CHANGED_FILES="$(git diff-tree --no-commit-id --name-only -r "$GITHUB_SHA")"
          test "$CHANGED_FILES" = "control/meta-ads-the-party-2026-08-15.diagnose"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_DEPLOY_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Read reconciliation Cloud Run logs only
        shell: bash
        run: |
          set -euo pipefail
          gcloud logging read \
            "resource.type=cloud_run_job AND resource.labels.job_name=${RECON_JOB_NAME} AND timestamp>=\"2026-08-14T07:12:10Z\" AND timestamp<=\"2026-08-14T07:12:30Z\"" \
            --project "$GCP_PROJECT_ID" \
            --order=asc \
            --limit=500 \
            --format=json > meta-ads-reconciliation-failure.json
          test -s meta-ads-reconciliation-failure.json
          echo '--- RECONCILIATION FAILURE ---'
          jq -r '.[] | [(.timestamp // ""), (.severity // ""), (.textPayload // .jsonPayload.message // .jsonPayload.error // (.jsonPayload|tostring) // "")] | @tsv' meta-ads-reconciliation-failure.json

      - name: Upload diagnostic evidence
        uses: actions/upload-artifact@v4
        with:
          name: meta-ads-reconciliation-failure-31778980116
          path: meta-ads-reconciliation-failure.json
          if-no-files-found: error
          retention-days: 30
