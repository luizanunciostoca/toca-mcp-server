name: Alerts Live Dump v3 2026-08-16

on:
  push:
    branches:
      - ops/alerts-email-readback-20260816
    paths:
      - .github/workflows/alerts-live-dump-v3-20260816.yml

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: toca-mcp-production
  GCP_INFRA_ADMIN_SERVICE_ACCOUNT: toca-mcp-infra-admin@toca-mcp-production.iam.gserviceaccount.com
  GCP_WORKLOAD_IDENTITY_PROVIDER: projects/990081828836/locations/global/workloadIdentityPools/github/providers/github-toca-mcp
  TEST_RUN: alerts-v3-31937476822-1

jobs:
  inspect:
    runs-on: ubuntu-latest
    environment: infrastructure-admin
    timeout-minutes: 10
    steps:
      - name: Authenticate infrastructure administrator
        uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed
        with:
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_INFRA_ADMIN_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@e427ad8a34f8676edf47cf7d7925499adf3eb74f
      - name: Read live alerts payload
        shell: bash
        run: |
          set -euo pipefail
          TOKEN="$(gcloud auth print-access-token)"
          HTTP="$(curl --silent --output /tmp/alerts.json --write-out '%{http_code}' -G -H "Authorization: Bearer ${TOKEN}" -H "x-goog-user-project: ${GCP_PROJECT_ID}" --data-urlencode 'page_size=100' "https://monitoring.googleapis.com/v3/projects/${GCP_PROJECT_ID}/alerts")"
          echo "ALERTS_HTTP=$HTTP"
          test "$HTTP" = 200
          echo '=== TOP LEVEL ==='
          jq -S 'keys' /tmp/alerts.json
          echo '=== MATCHES ==='
          jq -S --arg t "$TEST_RUN" '[.. | objects | select(tostring | contains($t))]' /tmp/alerts.json
          echo '=== ALERT SUMMARIES ==='
          jq -S '[.alerts[]? | {name,state,openTime,closeTime,policy,policyName,alertPolicy,alertPolicyName,displayName}]' /tmp/alerts.json
