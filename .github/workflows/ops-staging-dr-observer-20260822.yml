name: Ops Staging DR Observer 2026-08-22

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/ops-staging-dr-observer-20260822.yml'

permissions:
  contents: read
  actions: read

jobs:
  observe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Locate V5 emergency cleanup push run
        env:
          GH_TOKEN: ${{ github.token }}
          CLEANUP_HEAD_SHA: a67b9469535dbfd4725b1252f3c75627efe6e9a6
          OBSERVER_SEQUENCE: '5'
        shell: bash
        run: |
          set -euo pipefail
          gh api "/repos/${GITHUB_REPOSITORY}/actions/runs?head_sha=${CLEANUP_HEAD_SHA}&per_page=20" > /tmp/runs.json
          COUNT="$(jq '[.workflow_runs[] | select(.name=="Ops Staging DR V5 Emergency Cleanup 2026-08-22" and .event=="push")] | length' /tmp/runs.json)"
          test "$COUNT" -ge 1
          jq -r '.workflow_runs[] | select(.name=="Ops Staging DR V5 Emergency Cleanup 2026-08-22" and .event=="push") | "V5_EMERGENCY_CLEANUP_RUN_ID=\(.id) status=\(.status) conclusion=\(.conclusion // "") head=\(.head_sha)"' /tmp/runs.json
