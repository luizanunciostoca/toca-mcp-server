name: Instagram History Real Smoke

on:
  push:
    branches:
      - chore/validate-instagram-history-read

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: toca-mcp-production
  REGION: southamerica-east1
  REPOSITORY: toca-mcp
  RUNTIME_SERVICE_ACCOUNT: toca-mcp-runtime@toca-mcp-production.iam.gserviceaccount.com
  TOKEN_SECRET_ID: toca-meta-oauth-token
  INSTAGRAM_ACCOUNT_ID: '17841402033495654'
  READ_SMOKE_JOB: toca-instagram-history-read-smoke

jobs:
  real-read-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud with Workload Identity Federation
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/990081828836/locations/global/workloadIdentityPools/github/providers/github-toca-mcp
          service_account: toca-mcp-deployer@toca-mcp-production.iam.gserviceaccount.com

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker authentication
        run: gcloud auth configure-docker "$REGION-docker.pkg.dev" --quiet

      - name: Build isolated smoke image from current code
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/Dockerfile.instagram-history-smoke <<'DOCKER'
          FROM node:24-bookworm-slim AS build
          RUN corepack enable && corepack prepare pnpm@10.15.0 --activate
          WORKDIR /app
          COPY package.json pnpm-lock.yaml ./
          RUN pnpm install --frozen-lockfile
          COPY tsconfig.json ./
          COPY src ./src
          RUN pnpm build

          FROM node:24-bookworm-slim
          RUN corepack enable && corepack prepare pnpm@10.15.0 --activate
          WORKDIR /app
          COPY package.json pnpm-lock.yaml ./
          RUN pnpm install --prod --frozen-lockfile
          COPY --from=build /app/dist ./dist
          COPY .github/smoke/instagram-history-real-smoke.mjs ./.github/smoke/instagram-history-real-smoke.mjs
          USER node
          CMD ["node", ".github/smoke/instagram-history-real-smoke.mjs"]
          DOCKER

          IMAGE="$REGION-docker.pkg.dev/$PROJECT_ID/$REPOSITORY/instagram-history-smoke:${GITHUB_SHA}"
          docker build -f /tmp/Dockerfile.instagram-history-smoke -t "$IMAGE" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"

      - name: Execute real read smoke under runtime identity
        shell: bash
        run: |
          set -euo pipefail
          cleanup() {
            gcloud run jobs delete "$READ_SMOKE_JOB" --project "$PROJECT_ID" --region "$REGION" --quiet >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          gcloud run jobs deploy "$READ_SMOKE_JOB" \
            --image "$IMAGE" \
            --project "$PROJECT_ID" \
            --region "$REGION" \
            --service-account "$RUNTIME_SERVICE_ACCOUNT" \
            --set-env-vars "INSTAGRAM_ACCOUNT_ID=$INSTAGRAM_ACCOUNT_ID" \
            --set-secrets "META_ACCESS_TOKEN=$TOKEN_SECRET_ID:latest" \
            --max-retries 0 \
            --task-timeout 120s \
            --quiet

          EXECUTION="$(gcloud run jobs execute "$READ_SMOKE_JOB" --project "$PROJECT_ID" --region "$REGION" --wait --format='value(metadata.name)' --quiet)"
          test -n "$EXECUTION"
          echo "EXECUTION=$EXECUTION" >> "$GITHUB_ENV"

      - name: Read and validate smoke result
        shell: bash
        run: |
          set -euo pipefail
          sleep 3
          gcloud logging read \
            "resource.type=cloud_run_job AND resource.labels.job_name=$READ_SMOKE_JOB AND textPayload:mediaListOk" \
            --project "$PROJECT_ID" \
            --limit 5 \
            --freshness 15m \
            --format='value(textPayload)' > /tmp/smoke.log

          cat /tmp/smoke.log
          grep -q '"mediaListOk":true' /tmp/smoke.log
          grep -q '"instagramBasicGranted":true' /tmp/smoke.log
          echo 'Real Instagram history media.list passed. Insights scope/result is reported above without exposing the token.'
