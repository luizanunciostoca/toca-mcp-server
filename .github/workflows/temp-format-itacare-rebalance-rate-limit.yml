name: TEMP Format Itacare Rebalance Rate Limit

on:
  push:
    branches:
      - fix/meta-ads-itacare-rebalance-rate-limit-20260925
    paths:
      - 'src/meta-ads-itacare-rebalance-plus-1000-2026-09-25.ts'
      - '.github/workflows/temp-format-itacare-rebalance-rate-limit.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: true
          ref: fix/meta-ads-itacare-rebalance-rate-limit-20260925
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1
        with:
          version: 10.15.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write src/meta-ads-itacare-rebalance-plus-1000-2026-09-25.ts
      - name: Commit canonical formatting
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- src/meta-ads-itacare-rebalance-plus-1000-2026-09-25.ts; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/meta-ads-itacare-rebalance-plus-1000-2026-09-25.ts
          git commit -m "style(meta-ads): format rate-limit resilient rebalance"
          git push origin HEAD:fix/meta-ads-itacare-rebalance-rate-limit-20260925
